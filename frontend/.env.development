# 開発: 既定は Django（8000）へ直結。5173 からの fetch が CORS / プロキシ失敗しないようにする。
VITE_DEV_API_ORIGIN=http://127.0.0.1:8000
# Vite プロキシだけ使う（API_BASE を空にして /api をプロキシ）場合は true
# VITE_DEV_USE_PROXY=true
# Vite が /api と /ws をプロキシする先（DEV_USE_PROXY 利用時）
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000
