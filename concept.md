# LynkOS — コンセプト

## 最終目標（絶対条件）
以下をすべて満たすWebアプリを実装すること
- 同一ネットワーク(LAN)上の端末同士で動作
- ブラウザのみで利用(PWAとして動作)
- アカウント登録・ログイン不要
- 近くの端末を一覧表示し、ユーザーが選択して接続
- 接続時に短いセッションIDを双方に表示し、ユーザーが一致確認してから接続確立
- WebRTC DataChannel によるP2P通信を使用する
- ファイル本体はサーバーを一切経由しない(完全P2P)
- シグナリングは必要最小限にするが、接続成立に必要な情報は省略しない

---

## 非許容事項

- ファイルをサーバーにアップロード・中継する設計
- ログイン・認証機能の追加
- ネイティブアプリ(IOS/Electron)への置き換え
- WebRTCを使わない通信方式への変更
- 接続成立のために仕様を簡略化すること(例：HTTP転送にフォールバック)
- ICE candidateの省略・間引き
- DataChannel未確立状態でのファイル送信開始

---

## 概要

AirDrop に近い共有体験を、異なるOS間(iPhone/Windows)でブラウザのみを使って実現する。  
PWAとして動作し、同一ネットワーク上の端末同士を自動検出し、直接ファイル転送を行う。

---

## ユーザー体験（UXフロー）
1. デバイス検出
     - 同一ネットワーク、または同一シグナリングサーバーに接続している端末を一覧表示
2. 接続（ペアリング）
     1. 送信側が接続先デバイスを選択
     2. 双方に短いセッションIDを表示
     3. ユーザーが目視で一致確認
     4. 確認ダイアログで承認後、接続確立
3. 接続中UI
     - 「接続中」「待機中」など状態を明示
     - 不要な情報は表示せず、シンプルにする
4. ファイル送信
     - ファイルを選択して送信
     - 受信側がファイルごとに許可/拒否
5. 転送
     - 進捗を表示
     - 完了までユーザーに状態を伝える

---

## 通信設計（重要）

### WebRTC
- DataChannelを使用してファイルを転送
- 転送はチャンク単位で行う
- メモリに全ファイルを保持しない

### シグナリング(Django)
- WebSocketで実装
- 役割は以下に限定
     - デバイス検出
     - SDP交換
     - ICE candidate交換
ファイルデータは扱わない

### 接続方針
- ICE candidateは省略せず全て交換する
- STUNサーバーを使用する
- 必要に応じてTURNも使用する
- 接続の軽さより成立率を優先する

---

## 転送中の挙動（安定性）
- DataChannelが確立している場合、それを最優先とする
- 転送中は再接続処理を抑制する
- WebSocketの一時切断ではP2P接続を切断しない

---

## ファイルサイズと保存

### 共通方針
- チャンク分割で送信
- ストリーム処理を優先

### PC
- File System Access APIを使用
- ディスクへ直接書き込み
- 約1GBまで対応を目標

### iPhone/iPad
ブラウザ制約を考慮：
- ~100MB : 通常動作
- 100MB~300MB : 注意表示　例：容量が大きめです
- 300MB以上 : 警告表示（続行可否はUIで明示）

---

## 技術スタック

| レイヤー | 技術 |
|----------|------|
| クライアント | React + Vite（PWA） |
| 通信 | WebRTC DataChannel |
| シグナリング | Django Channels（WebSocket） |
| バックエンド | Django + Daphne |

---

## 設計原則

- シグナリングは最小限だが、省略はしない
- 誤接続を防ぐため、ユーザー確認を必須とする
- 環境差(iOS/ブラウザ制限)を前提に設計する