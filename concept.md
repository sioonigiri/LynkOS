# LynkOS — コンセプト

## 最終目標（絶対条件）
以下をすべて満たすWebアプリを実装すること
- 同一ネットワーク(LAN)上の端末同士で動作
- ブラウザのみで利用(PWAとして動作)
- アカウント登録・ログイン不要
- 同一Wi-Fiに接続している端末を自動検出し一覧表示
- ユーザーが送信先デバイスを選択して接続
- 受信側に許可/拒否のポップアップを表示
- 許可後にのみ転送を開始する
- WebRTC DataChannel によるP2P通信を使用する
- ファイル本体はサーバーを一切経由しない(完全P2P)
- シグナリングは接続成立に必要な最小限の情報のみ扱う

---

## 非許容事項

- ファイルをサーバーにアップロード・中継する設計
- ログイン・認証機能の追加
- ネイティブアプリ(IOS/Electron)への置き換え
- WebRTCを使わない通信方式への変更
- 接続成立のために仕様を簡略化すること(例：HTTP転送にフォールバック)
- ICE candidateの省略・間引き
- DataChannel未確立状態でのファイル送信開始

---

## 概要

AirDrop に近い共有体験を、異なるOS間(iPhone/Windows)でブラウザのみを使って実現する。  
同一Wi-Fi上の端末を自動検出し、ユーザーの選択によって直接ファイル転送を行う。

---

## ユーザー体験（UXフロー）
1. デバイス検出
     - 同一Wi-Fi、または同一シグナリングサーバーに接続している端末を自動一覧表示
2. 送信操作
     - ユーザーがファイル・画像を選択
     - 一覧から送信先デバイスを選択
3. 受信確認
     - 受信側にポップアップを表示
          - 「〇〇からファイル送信」
          - 許可/拒否
     - 拒否時は即キャンセル
4. 接続確立
     - 許可された場合のみWebRTC接続を確立
5. 転送
     - DataChannelでファイル転送開始
     - 進捗を表示
     - 完了に通知

---

## 通信設計（重要）

### WebRTC
- DataChannelを使用してファイルを転送
- 転送はチャンク単位で行う
- メモリに全ファイルを保持しない
- 接続確立は「受信側の許可後」に開始する

### シグナリング(Django)
- WebSocketで実装
- 役割は以下に限定
     - デバイス検出（接続中クライアント一覧）
     - 接続リクエスト通知（送信要求）
     - SDP交換
     - ICE candidate交換
- ファイルデータは扱わない

### 接続フロー
1. 送信側が「接続リクエスト」を送信
2. 受信側にポップアップ表示
3. ユーザーが許可
4. その後にWebRTCネゴシエーション開始
- セッションIDによる手動確認は行わない

---

## 転送中の挙動（安定性）
- DataChannelが確立後はP2P通信を最優先
- 転送中は再接続処理を抑制
- WebSocket切断では転送を切断しない

---

## ファイルサイズと保存

### 共通方針
- チャンク分割で送信
- ストリーム処理を優先

### PC
- File System Access APIを使用
- ディスクへ直接書き込み
- 約1GBまで対応を目標

### iPhone/iPad
ブラウザ制約を考慮：
- ~100MB : 通常動作
- 100MB~300MB : 注意表示　例：容量が大きめです
- 300MB以上 : 警告表示（続行可否はUIで明示）

---

## 技術スタック

| レイヤー | 技術 |
|----------|------|
| クライアント | React + Vite（PWA） |
| 通信 | WebRTC DataChannel |
| シグナリング | Django Channels（WebSocket） |
| バックエンド | Django + Daphne |

---

## 設計原則
- ユーザー操作は最小限（直感的なフロー）
- 接続は「許可ベース」で安全に行う
- シグナリングは最小限だが省略はしない
- 環境差(iOS/ブラウザ制限)を前提に設計する